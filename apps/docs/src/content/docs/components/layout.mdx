---
title: Layout Components
description: Flexible layout components for building responsive React Native interfaces
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

Layout components provide flexible and powerful tools for organizing and structuring your React Native application's UI. These components are built with theme support and offer consistent spacing, alignment, and styling options.

## Available Components

- **HStack** - Horizontal stack layout
- **VStack** - Vertical stack layout
- **ZStack** - Absolute positioned stack
- **Grid** - Responsive grid layout
- **Center** - Centers content both horizontally and vertically
- **Box** - Themed container with multiple variants

## HStack

Arranges children horizontally in a row with flexible spacing and alignment options.

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `spacing` | `SpacingKey` | - | Space between children |
| `align` | `FlexAlignType` | `'stretch'` | Cross-axis alignment |
| `justify` | `FlexJustifyType` | `'flex-start'` | Main-axis alignment |
| `wrap` | `boolean` | `false` | Allow children to wrap |
| `padding` | `SpacingKey` | - | Internal padding |
| `margin` | `SpacingKey` | - | External margin |
| `backgroundColor` | `ColorKey` | - | Background color |
| `borderRadius` | `BorderRadiusKey` | - | Border radius |
| `themed` | `boolean` | `false` | Apply theme background |

### Example

<Tabs>
<TabItem label="Basic Usage">

```tsx
import { HStack, Box, Text } from 'rnc-theme';

function HorizontalLayout() {
  return (
    <HStack spacing="md" justify="space-between" padding="lg">
      <Box flex={1} padding="md" backgroundColor="primary" borderRadius="md">
        <Text style={{ color: 'white', textAlign: 'center' }}>Left</Text>
      </Box>
      <Box flex={2} padding="md" backgroundColor="secondary" borderRadius="md">
        <Text style={{ color: 'white', textAlign: 'center' }}>Right (Wider)</Text>
      </Box>
    </HStack>
  );
}
```

</TabItem>
<TabItem label="With Alignment">

```tsx
import { HStack, Box, Text } from 'rnc-theme';

function AlignedLayout() {
  return (
    <HStack spacing="sm" align="center" justify="center" padding="lg">
      <Box padding="sm" backgroundColor="success" borderRadius="sm">
        <Text style={{ color: 'white' }}>✓ Success</Text>
      </Box>
      <Box padding="sm" backgroundColor="warning" borderRadius="sm">
        <Text style={{ color: 'white' }}>⚠ Warning</Text>
      </Box>
      <Box padding="sm" backgroundColor="error" borderRadius="sm">
        <Text style={{ color: 'white' }}>✗ Error</Text>
      </Box>
    </HStack>
  );
}
```

</TabItem>
</Tabs>

## VStack

Arranges children vertically in a column with flexible spacing and alignment options.

### Props

Same props as HStack, but arranges children vertically.

### Example

<Tabs>
<TabItem label="Basic Usage">

```tsx
import { VStack, Box, Text } from 'rnc-theme';

function VerticalLayout() {
  return (
    <VStack spacing="md" padding="lg">
      <Box padding="md" backgroundColor="primary" borderRadius="md">
        <Text style={{ color: 'white', textAlign: 'center' }}>Header</Text>
      </Box>
      <Box flex={1} padding="md" backgroundColor="surface" borderRadius="md">
        <Text style={{ textAlign: 'center' }}>Content Area</Text>
      </Box>
      <Box padding="md" backgroundColor="secondary" borderRadius="md">
        <Text style={{ color: 'white', textAlign: 'center' }}>Footer</Text>
      </Box>
    </VStack>
  );
}
```

</TabItem>
<TabItem label="Status List">

```tsx
import { VStack, HStack, Box, Text } from 'rnc-theme';

function StatusList() {
  return (
    <VStack spacing="sm" padding="lg">
      <Box padding="md" backgroundColor="success" borderRadius="md">
        <HStack spacing="sm" align="center">
          <Text style={{ fontSize: 16 }}>✅</Text>
          <VStack flex={1}>
            <Text style={{ color: 'white', fontWeight: 'bold' }}>Task Completed</Text>
            <Text style={{ color: 'white', fontSize: 12 }}>All tests passed</Text>
          </VStack>
        </HStack>
      </Box>

      <Box padding="md" backgroundColor="warning" borderRadius="md">
        <HStack spacing="sm" align="center">
          <Text style={{ fontSize: 16 }}>⚠️</Text>
          <VStack flex={1}>
            <Text style={{ color: 'white', fontWeight: 'bold' }}>Warning</Text>
            <Text style={{ color: 'white', fontSize: 12 }}>Check configuration</Text>
          </VStack>
        </HStack>
      </Box>
    </VStack>
  );
}
```

</TabItem>
</Tabs>

## ZStack

Positions children absolutely, allowing them to overlap. Useful for creating layered interfaces.

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `padding` | `SpacingKey` | - | Internal padding |
| `margin` | `SpacingKey` | - | External margin |
| `backgroundColor` | `ColorKey` | - | Background color |
| `borderRadius` | `BorderRadiusKey` | - | Border radius |
| `themed` | `boolean` | `false` | Apply theme background |

### Example

```tsx
import { ZStack, Box, Text } from 'rnc-theme';

function OverlayLayout() {
  return (
    <ZStack height={200} backgroundColor="surface" borderRadius="lg">
      <Box
        position="absolute"
        top={0}
        left={0}
        right={0}
        bottom={0}
        backgroundColor="primary"
        borderRadius="lg"
      />
      <Box
        position="absolute"
        top={20}
        left={20}
        padding="md"
        backgroundColor="white"
        borderRadius="md"
      >
        <Text style={{ color: 'black', fontWeight: 'bold' }}>Overlay Content</Text>
      </Box>
    </ZStack>
  );
}
```

## Grid

Creates a responsive grid layout with configurable columns and spacing.

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `columns` | `number` | `2` | Number of columns |
| `spacing` | `SpacingKey` | - | Space between grid items |
| `align` | `FlexAlignType` | `'stretch'` | Cross-axis alignment |
| `justify` | `FlexJustifyType` | `'flex-start'` | Main-axis alignment |
| `padding` | `SpacingKey` | - | Internal padding |
| `margin` | `SpacingKey` | - | External margin |
| `backgroundColor` | `ColorKey` | - | Background color |
| `borderRadius` | `BorderRadiusKey` | - | Border radius |
| `themed` | `boolean` | `false` | Apply theme background |

### Example

<Tabs>
<TabItem label="2 Column Grid">

```tsx
import { Grid, Box, Text, Center } from 'rnc-theme';

function TwoColumnGrid() {
  return (
    <Grid columns={2} spacing="sm" padding="lg">
      <Box padding="md" backgroundColor="primary" borderRadius="md">
        <Center>
          <Text style={{ color: 'white', fontWeight: 'bold' }}>📱 Mobile</Text>
          <Text style={{ color: 'white', fontSize: 12 }}>Responsive Design</Text>
        </Center>
      </Box>

      <Box padding="md" backgroundColor="secondary" borderRadius="md">
        <Center>
          <Text style={{ color: 'white', fontWeight: 'bold' }}>💻 Desktop</Text>
          <Text style={{ color: 'white', fontSize: 12 }}>Web Application</Text>
        </Center>
      </Box>

      <Box padding="md" backgroundColor="success" borderRadius="md">
        <Center>
          <Text style={{ color: 'white', fontWeight: 'bold' }}>🎨 Design</Text>
          <Text style={{ color: 'white', fontSize: 12 }}>UI/UX Components</Text>
        </Center>
      </Box>

      <Box padding="md" backgroundColor="warning" borderRadius="md">
        <Center>
          <Text style={{ color: 'white', fontWeight: 'bold' }}>⚡ Performance</Text>
          <Text style={{ color: 'white', fontSize: 12 }}>Fast & Optimized</Text>
        </Center>
      </Box>
    </Grid>
  );
}
```

</TabItem>
<TabItem label="3 Column Grid">

```tsx
import { Grid, Box, Text, Center } from 'rnc-theme';

function ThreeColumnGrid() {
  return (
    <Grid columns={3} spacing="xs" padding="lg">
      <Box padding="sm" backgroundColor="#FF6B6B" borderRadius="sm">
        <Center>
          <Text style={{ color: 'white', fontSize: 11, fontWeight: 'bold' }}>🏠 Home</Text>
        </Center>
      </Box>

      <Box padding="sm" backgroundColor="#4ECDC4" borderRadius="sm">
        <Center>
          <Text style={{ color: 'white', fontSize: 11, fontWeight: 'bold' }}>👤 Profile</Text>
        </Center>
      </Box>

      <Box padding="sm" backgroundColor="#45B7D1" borderRadius="sm">
        <Center>
          <Text style={{ color: 'white', fontSize: 11, fontWeight: 'bold' }}>⚙️ Settings</Text>
        </Center>
      </Box>

      <Box padding="sm" backgroundColor="#96CEB4" borderRadius="sm">
        <Center>
          <Text style={{ color: 'white', fontSize: 11, fontWeight: 'bold' }}>📊 Analytics</Text>
        </Center>
      </Box>

      <Box padding="sm" backgroundColor="#FFEAA7" borderRadius="sm">
        <Center>
          <Text style={{ color: '#333', fontSize: 11, fontWeight: 'bold' }}>💬 Messages</Text>
        </Center>
      </Box>

      <Box padding="sm" backgroundColor="#DDA0DD" borderRadius="sm">
        <Center>
          <Text style={{ color: 'white', fontSize: 11, fontWeight: 'bold' }}>🔔 Notifications</Text>
        </Center>
      </Box>
    </Grid>
  );
}
```

</TabItem>
</Tabs>

## Center

Centers its children both horizontally and vertically.

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `padding` | `SpacingKey` | - | Internal padding |
| `margin` | `SpacingKey` | - | External margin |
| `backgroundColor` | `ColorKey` | - | Background color |
| `borderRadius` | `BorderRadiusKey` | - | Border radius |
| `themed` | `boolean` | `false` | Apply theme background |

### Example

```tsx
import { Center, Text } from 'rnc-theme';

function CenteredContent() {
  return (
    <Center
      height={200}
      backgroundColor="primary"
      borderRadius="lg"
      padding="lg"
    >
      <Text style={{ color: 'white', fontSize: 24, fontWeight: 'bold' }}>
        Perfectly Centered
      </Text>
      <Text style={{ color: 'white', fontSize: 14, opacity: 0.8, marginTop: 8 }}>
        Both horizontally and vertically
      </Text>
    </Center>
  );
}
```

## Box

A versatile container component with multiple variants and styling options.

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'default' \| 'card' \| 'surface'` | `'default'` | Box style variant |
| `padding` | `SpacingKey` | - | Internal padding |
| `margin` | `SpacingKey` | - | External margin |
| `backgroundColor` | `ColorKey` | - | Background color |
| `borderRadius` | `BorderRadiusKey` | - | Border radius |
| `borderWidth` | `number` | - | Border width |
| `borderColor` | `ColorKey` | - | Border color |
| `shadowOpacity` | `number` | - | Shadow opacity |
| `elevation` | `number` | - | Android elevation |
| `themed` | `boolean` | `true` | Apply theme styling |

### Variants

- **default**: Basic surface background
- **card**: Elevated card with shadow and padding
- **surface**: Background color variant

### Example

<Tabs>
<TabItem label="Basic Box">

```tsx
import { Box, Text } from 'rnc-theme';

function BasicBox() {
  return (
    <Box
      padding="lg"
      backgroundColor="surface"
      borderRadius="md"
      borderWidth={1}
      borderColor="border"
    >
      <Text>Basic box with border and padding</Text>
    </Box>
  );
}
```

</TabItem>
<TabItem label="Card Variant">

```tsx
import { Box, Text, VStack } from 'rnc-theme';

function CardBox() {
  return (
    <Box variant="card" margin="md">
      <VStack spacing="sm">
        <Text style={{ fontSize: 18, fontWeight: 'bold' }}>Card Title</Text>
        <Text style={{ fontSize: 14, opacity: 0.7 }}>
          This is a card variant with built-in shadow and padding.
        </Text>
      </VStack>
    </Box>
  );
}
```

</TabItem>
<TabItem label="Custom Styling">

```tsx
import { Box, Text } from 'rnc-theme';

function CustomBox() {
  return (
    <Box
      padding="xl"
      backgroundColor="primary"
      borderRadius="lg"
      shadowOpacity={0.3}
      elevation={5}
    >
      <Text style={{ color: 'white', textAlign: 'center', fontWeight: 'bold' }}>
        Custom styled box with shadow
      </Text>
    </Box>
  );
}
```

</TabItem>
</Tabs>

## Common Patterns

### Card Layout

```tsx
import { VStack, HStack, Box, Text, Center } from 'rnc-theme';

function ProfileCard() {
  return (
    <Box variant="card" margin="md">
      <VStack spacing="md">
        <HStack spacing="md" align="center">
          <Box
            width={60}
            height={60}
            backgroundColor="primary"
            borderRadius="full"
          >
            <Center flex={1}>
              <Text style={{ color: 'white', fontSize: 20, fontWeight: 'bold' }}>
                JD
              </Text>
            </Center>
          </Box>

          <VStack flex={1} spacing="xs">
            <Text style={{ fontSize: 18, fontWeight: 'bold' }}>John Doe</Text>
            <Text style={{ fontSize: 14, opacity: 0.7 }}>Software Developer</Text>
          </VStack>
        </HStack>

        <Text style={{ fontSize: 14, lineHeight: 20 }}>
          Passionate developer with 5+ years of experience in React Native
          and mobile app development.
        </Text>
      </VStack>
    </Box>
  );
}
```

### Dashboard Grid

```tsx
import { Grid, Box, Text, Center, VStack } from 'rnc-theme';

function DashboardGrid() {
  const stats = [
    { title: 'Users', value: '1,234', icon: '👥', color: '#4ECDC4' },
    { title: 'Revenue', value: '$12.5K', icon: '💰', color: '#45B7D1' },
    { title: 'Orders', value: '856', icon: '📦', color: '#96CEB4' },
    { title: 'Growth', value: '+15%', icon: '📈', color: '#FFEAA7' },
  ];

  return (
    <Grid columns={2} spacing="md" padding="lg">
      {stats.map((stat, index) => (
        <Box
          key={index}
          padding="lg"
          backgroundColor={stat.color}
          borderRadius="lg"
        >
          <VStack spacing="sm" align="center">
            <Text style={{ fontSize: 24 }}>{stat.icon}</Text>
            <Text style={{
              color: 'white',
              fontSize: 20,
              fontWeight: 'bold'
            }}>
              {stat.value}
            </Text>
            <Text style={{
              color: 'white',
              fontSize: 12,
              opacity: 0.9
            }}>
              {stat.title}
            </Text>
          </VStack>
        </Box>
      ))}
    </Grid>
  );
}
```

## Best Practices

1. **Use semantic spacing**: Prefer theme spacing keys (`xs`, `sm`, `md`, `lg`, `xl`) over hardcoded values
2. **Leverage flex properties**: Use `flex` prop for responsive layouts
3. **Combine components**: Mix different layout components for complex interfaces
4. **Theme consistency**: Use `themed` prop and theme colors for consistent styling
5. **Performance**: Avoid deep nesting when possible, use flat structures with Grid for better performance

## Accessibility

All layout components support standard React Native accessibility props:

```tsx
<Box
  accessible={true}
  accessibilityRole="button"
  accessibilityLabel="Profile card"
  accessibilityHint="Tap to view profile details"
>
  {/* Content */}
</Box>
```

## TypeScript Support

All components are fully typed with TypeScript:

```tsx
import type { StackProps, BoxProps, GridProps } from 'rnc-theme';

// Custom component with proper typing
interface CustomLayoutProps extends StackProps {
  title: string;
  subtitle?: string;
}

function CustomLayout({ title, subtitle, ...stackProps }: CustomLayoutProps) {
  return (
    <VStack {...stackProps}>
      <Text style={{ fontSize: 18, fontWeight: 'bold' }}>{title}</Text>
      {subtitle && (
        <Text style={{ fontSize: 14, opacity: 0.7 }}>{subtitle}</Text>
      )}
    </VStack>
  );
}
```