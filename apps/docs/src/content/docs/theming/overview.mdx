---
title: Theming Overview
description: Complete guide to the RNC Theme system - a powerful, type-safe theming solution for React Native applications.
sidebar:
  order: 1
---

{/* TODO untuk besok */}

import { Card, CardGrid } from '@astrojs/starlight/components';
import { Tabs, TabItem } from '@astrojs/starlight/components';
import { Code } from '@astrojs/starlight/components';
import { Badge } from '@astrojs/starlight/components';

# Theming Overview

RNC Theme is a comprehensive theming system designed for React Native applications. It provides type-safe, dynamic theme switching with extensive customization capabilities.

## Key Features

<CardGrid>
  <Card title="🎨 Type-Safe Theming" icon="star">
    Full TypeScript support with intelligent autocomplete for all theme properties and values.
  </Card>
  <Card title="🌓 Dynamic Theme Switching" icon="moon">
    Seamless switching between light, dark, and system themes with smooth animations.
  </Card>
  <Card title="🎯 High Customization" icon="setting">
    Extensive customization options for colors, typography, spacing, and component styles.
  </Card>
  <Card title="💾 Persistent Storage" icon="document">
    Automatic theme persistence across app sessions with AsyncStorage integration.
  </Card>
  <Card title="🎭 Built-in Presets" icon="palette">
    Multiple pre-designed theme presets including Material, Neon, Ocean, and more.
  </Card>
  <Card title="📱 Platform Optimized" icon="mobile">
    Optimized for both iOS and Android with platform-specific adaptations.
  </Card>
</CardGrid>

## Architecture Overview

The RNC Theme system is built around several core concepts:

### Theme Structure

Every theme consists of five main categories:

<Tabs>
  <TabItem label="Colors">
    ```typescript
    colors: {
      primary: string;
      secondary: string;
      background: string;
      surface: string;
      text: string;
      textSecondary: string;
      border: string;
      error: string;
      warning: string;
      success: string;
      info: string;
      muted: string;
      accent: string;
      destructive: string;
    }
    ```
  </TabItem>
  <TabItem label="Typography">
    ```typescript
    typography: {
      caption: { fontSize: number; lineHeight: number; fontWeight: string };
      small: { fontSize: number; lineHeight: number; fontWeight: string };
      body: { fontSize: number; lineHeight: number; fontWeight: string };
      subtitle: { fontSize: number; lineHeight: number; fontWeight: string };
      title: { fontSize: number; lineHeight: number; fontWeight: string };
      heading: { fontSize: number; lineHeight: number; fontWeight: string };
    }
    ```
  </TabItem>
  <TabItem label="Spacing">
    ```typescript
    spacing: {
      xs: number;  // 4
      sm: number;  // 8
      md: number;  // 16
      lg: number;  // 24
      xl: number;  // 32
      xxl: number; // 48
    }
    ```
  </TabItem>
  <TabItem label="Components">
    ```typescript
    components: {
      height: { xs: number; sm: number; md: number; lg: number; xl: number };
      padding: { xs: number; sm: number; md: number; lg: number; xl: number };
      borderRadius: { xs: number; sm: number; md: number; lg: number; xl: number; full: number };
    }
    ```
  </TabItem>
  <TabItem label="Font Sizes">
    ```typescript
    fontSizes: {
      xs: number;  // 12
      sm: number;  // 14
      md: number;  // 16
      lg: number;  // 18
      xl: number;  // 20
      xxl: number; // 24
    }
    ```
  </TabItem>
</Tabs>

## Core Components

### Theme Provider

The `RNCProvider` is the root component that provides theme context to your entire application:

```tsx
import { RNCProvider } from 'rnc-theme';

export default function App() {
  return (
    <RNCProvider defaultTheme="system">
      {/* Your app content */}
    </RNCProvider>
  );
}
```

### Theme Hooks

Two primary hooks for accessing and using themes:

<Tabs>
  <TabItem label="useTheme">
    ```tsx
    import { useTheme } from 'rnc-theme';

    function MyComponent() {
      const { theme, mode, setMode, updateCustomTheme } = useTheme();

      return (
        <View style={{ backgroundColor: theme.colors.background }}>
          <Text style={{ color: theme.colors.text }}>Hello World</Text>
        </View>
      );
    }
    ```
  </TabItem>
  <TabItem label="useThemedStyles">
    ```tsx
    import { useThemedStyles } from 'rnc-theme';

    function MyComponent() {
      const styles = useThemedStyles(createStyles);

      return <View style={styles.container} />;
    }

    const createStyles = (theme: Theme) => ({
      container: {
        backgroundColor: theme.colors.background,
        padding: theme.spacing.md,
        borderRadius: theme.components.borderRadius.md,
      },
    });
    ```
  </TabItem>
</Tabs>

### Theme Registry

Manage multiple theme presets with the theme registry:

```tsx
import { themeRegistry } from 'rnc-theme';

// Register a custom theme
themeRegistry.register('myTheme', {
  colors: {
    primary: '#FF6B6B',
    secondary: '#4ECDC4',
    // ... other colors
  },
  // ... other theme properties
});

// Apply the theme
const { setActivePreset } = useTheme();
setActivePreset('myTheme');
```

## Built-in Theme Presets

RNC Theme comes with several beautiful preset themes:

<CardGrid stagger>
  <Card title="Material" icon="star">
    <Badge text="Default" variant="tip" />
    Google Material Design inspired theme with clean, modern aesthetics.
  </Card>
  <Card title="Neon" icon="star">
    <Badge text="Vibrant" variant="note" />
    High-contrast theme with bright, electric colors perfect for gaming apps.
  </Card>
  <Card title="Ocean" icon="star">
    <Badge text="Calm" variant="success" />
    Soothing blue and teal tones inspired by ocean depths.
  </Card>
  <Card title="Sunset" icon="star">
    <Badge text="Warm" variant="caution" />
    Warm oranges and pinks reminiscent of a beautiful sunset.
  </Card>
  <Card title="Forest" icon="star">
    <Badge text="Natural" variant="success" />
    Earth tones and greens inspired by nature.
  </Card>
  <Card title="Galaxy" icon="star">
    <Badge text="Cosmic" variant="note" />
    Deep purples and blues with cosmic inspiration.
  </Card>
  <Card title="Vintage" icon="star">
    <Badge text="Classic" variant="tip" />
    Muted, classic colors with a retro feel.
  </Card>
  <Card title="Cyberpunk" icon="star">
    <Badge text="Futuristic" variant="danger" />
    High-tech aesthetic with neon accents.
  </Card>
</CardGrid>

## Theme Modes

Supports three theme modes:

- **Light Mode**: Optimized for bright environments
- **Dark Mode**: Optimized for low-light environments
- **System Mode**: Automatically follows device settings

```tsx
const { mode, setMode } = useTheme();

// Switch to dark mode
setMode('dark');

// Follow system preference
setMode('system');
```

## Performance Optimizations

### Memoization
All theme calculations are memoized to prevent unnecessary re-renders:

```tsx
const styles = useThemedStyles(createStyles); // Memoized automatically
```

### Lazy Loading
Theme presets are loaded on-demand to reduce initial bundle size.

### Efficient Updates
Only components using changed theme properties re-render during theme switches.

## Accessibility

RNC Theme includes built-in accessibility features:

- **High Contrast Support**: Automatic contrast adjustments
- **Reduced Motion**: Respects system motion preferences
- **Screen Reader Support**: Semantic color naming
- **Focus Indicators**: Clear focus states for all interactive elements

## Integration Examples

### Basic Setup

```tsx
import React from 'react';
import { RNCProvider } from 'rnc-theme';
import { NavigationContainer } from '@react-navigation/native';

export default function App() {
  return (
    <RNCProvider
      defaultTheme="system"
      persistTheme={true}
    >
      <NavigationContainer>
        {/* Your navigation structure */}
      </NavigationContainer>
    </RNCProvider>
  );
}
```

### Custom Theme Configuration

```tsx
const customThemeConfig = {
  colors: {
    primary: '#6366F1',
    secondary: '#8B5CF6',
    background: '#F8FAFC',
    surface: '#FFFFFF',
    // ... other colors
  },
  components: {
    borderRadius: {
      xs: 4,
      sm: 6,
      md: 8,
      lg: 12,
      xl: 16,
      full: 9999,
    },
  },
};

<RNCProvider customTheme={customThemeConfig}>
  {/* Your app */}
</RNCProvider>
```

## Next Steps

<CardGrid>
  <Card title="Theme Provider Setup" icon="rocket">
    Learn how to configure and customize the RNCProvider for your app.
    [Setup Guide →](/theming/theme-provider)
  </Card>
  <Card title="Custom Themes" icon="palette">
    Create your own custom themes and color schemes.
    [Custom Themes →](/theming/custom-theme)
  </Card>
  <Card title="Theme Registry" icon="list">
    Manage multiple themes with the theme registry system.
    [Registry Guide →](/theming/theme-registry)
  </Card>
  <Card title="Dynamic Switching" icon="setting">
    Implement smooth theme switching in your app.
    [Switching Guide →](/theming/theme-switching)
  </Card>
</CardGrid>

## Resources

- [Theme Hooks Reference](/hooks/overview)
- [Component Theming](/components/overview)
- [Migration Guide](/getting-started/setup-guide#migration)
- [Best Practices](/getting-started/setup-guide#best-practices)